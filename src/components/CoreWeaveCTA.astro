---
// CoreWeave-inspired call-to-action section
---

<section class="cw-cta">
  <div class="cw-cta-background">
    <div class="cw-cta-gradient-1"></div>
    <div class="cw-cta-gradient-2"></div>
  </div>
  <div class="cw-cta-container">
    <div class="cw-cta-content">
      <h2 class="cw-gradient-text">Ready to Build?</h2>
      <p>Join thousands of developers who trust Starlight for their documentation needs. Get started in minutes, scale to any size.</p>
      <div class="cw-cta-actions">
        <a href="/getting-started/" class="cw-button cw-button-primary">
          Start Building
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
        <a href="/guides/" class="cw-button cw-button-outline">
          View Examples
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M8 1L15 8L8 15L1 8L8 1Z" stroke="currentColor" stroke-width="2" stroke-linejoin="round"/>
          </svg>
        </a>
      </div>
    </div>
    <div class="cw-cta-stats">
      <div class="cw-stat">
        <div class="cw-stat-number">50k+</div>
        <div class="cw-stat-label">Projects Built</div>
      </div>
      <div class="cw-stat">
        <div class="cw-stat-number">99.9%</div>
        <div class="cw-stat-label">Uptime</div>
      </div>
      <div class="cw-stat">
        <div class="cw-stat-number">< 1s</div>
        <div class="cw-stat-label">Load Time</div>
      </div>
    </div>
  </div>
</section>

<style>
.cw-cta {
  padding: 6rem 0;
  position: relative;
  overflow: hidden;
  background: linear-gradient(135deg, var(--cw-primary-blue) 0%, var(--cw-primary-blue-hover) 50%, var(--cw-light-blue) 100%);
  color: white;
}

.cw-cta-background {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  pointer-events: none;
}

.cw-cta-gradient-1,
.cw-cta-gradient-2 {
  position: absolute;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.1);
  animation: float 8s ease-in-out infinite;
}

.cw-cta-gradient-1 {
  width: 300px;
  height: 300px;
  top: -150px;
  right: -100px;
  animation-delay: -2s;
}

.cw-cta-gradient-2 {
  width: 200px;
  height: 200px;
  bottom: -100px;
  left: -50px;
  animation-delay: -4s;
}

.cw-cta-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 2rem;
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 4rem;
  align-items: center;
  position: relative;
  z-index: 1;
}

.cw-cta-content {
  max-width: 600px;
}

.cw-cta-content h2 {
  font-size: 2.5rem;
  font-weight: 700;
  margin-bottom: 1rem;
  background: linear-gradient(135deg, white 0%, rgba(255, 255, 255, 0.8) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.cw-cta-content p {
  font-size: 1.125rem;
  line-height: 1.6;
  margin-bottom: 2rem;
  opacity: 0.9;
}

.cw-cta-actions {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

.cw-cta .cw-button-primary {
  background: rgba(255, 255, 255, 0.2);
  color: white;
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.3);
}

.cw-cta .cw-button-primary:hover {
  background: rgba(255, 255, 255, 0.3);
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
}

.cw-cta .cw-button-outline {
  background: transparent;
  color: white;
  border-color: rgba(255, 255, 255, 0.5);
}

.cw-cta .cw-button-outline:hover {
  background: rgba(255, 255, 255, 0.1);
  transform: translateY(-2px);
}

.cw-cta-stats {
  display: flex;
  flex-direction: column;
  gap: 2rem;
}

.cw-stat {
  text-align: center;
  padding: 1.5rem;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 12px;
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.cw-stat-number {
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
}

.cw-stat-label {
  font-size: 0.875rem;
  opacity: 0.8;
  font-weight: 500;
}

@keyframes float {
  0%, 100% { 
    transform: translateY(0px) rotate(0deg); 
  }
  50% { 
    transform: translateY(-20px) rotate(5deg); 
  }
}

@media (max-width: 768px) {
  .cw-cta {
    padding: 4rem 0;
  }
  
  .cw-cta-container {
    grid-template-columns: 1fr;
    gap: 3rem;
    text-align: center;
  }
  
  .cw-cta-content h2 {
    font-size: 2rem;
  }
  
  .cw-cta-stats {
    flex-direction: row;
    justify-content: center;
  }
  
  .cw-stat {
    flex: 1;
    max-width: 120px;
  }
  
  .cw-stat-number {
    font-size: 1.5rem;
  }
}
</style>